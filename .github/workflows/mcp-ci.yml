name: MCP CI
on:
  push:
    branches: [main]
    paths:
      - 'services/mcp-server/**'
      - 'render.yaml'
      - '.github/workflows/mcp-ci.yml'
  pull_request:
    paths:
      - 'services/mcp-server/**'
      - 'render.yaml'
      - '.github/workflows/mcp-ci.yml'

permissions:
  contents: read

concurrency:
  group: mcp-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/mcp-server
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: services/mcp-server/package-lock.json
      - run: npm ci --ignore-scripts
      - name: Dependency remediation probe
        shell: bash
        run: |
          set -euo pipefail
          npm ls fast-uri qs --all > dependency-tree-before.txt || true
          cp package-lock.json package-lock.before.json
          npm audit fix --package-lock-only --ignore-scripts
          npm ci --ignore-scripts
          npm ls fast-uri qs --all > dependency-tree-after.txt || true
          npm audit --audit-level=high --json > npm-audit-after-fix.json || true
          git diff -- package-lock.json > package-lock-security-fix.patch || true
      - name: Upload dependency remediation candidate
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: dependency-remediation-candidate
          path: |
            services/mcp-server/package-lock.json
            services/mcp-server/package-lock.before.json
            services/mcp-server/dependency-tree-before.txt
            services/mcp-server/dependency-tree-after.txt
            services/mcp-server/npm-audit-after-fix.json
            services/mcp-server/package-lock-security-fix.patch
      - name: Require committed dependency remediation
        shell: bash
        run: git diff --exit-code -- package-lock.json
      - name: Typecheck
        shell: bash
        run: |
          set -o pipefail
          npm run typecheck 2>&1 | tee typecheck.log
      - name: Upload typecheck diagnostics
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: typecheck-diagnostics
          path: services/mcp-server/typecheck.log
      - run: npm test
      - name: Build
        shell: bash
        run: |
          set -o pipefail
          npm run build 2>&1 | tee build.log
      - name: Upload build diagnostics
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: build-diagnostics
          path: services/mcp-server/build.log
      # Security remains fail-closed. It runs after quality checks so the CI record
      # preserves test/build evidence even when a dependency advisory vetoes release.
      - name: Dependency audit
        shell: bash
        run: |
          set -o pipefail
          npm audit --audit-level=high --json 2>&1 | tee npm-audit.json
      - name: Upload dependency audit diagnostics
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: dependency-audit-diagnostics
          path: services/mcp-server/npm-audit.json
