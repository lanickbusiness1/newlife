import type {Alert} from '../../domain/types';export function AlertsView({alerts,onAcknowledge}:{alerts:Alert[];onAcknowledge:(id:string)=>void}){return <section><h2>Alertes explicables</h2>{alerts.length===0?<p className="empty">Aucune alerte active.</p>:alerts.map(a=><article className="alert-card" key={a.alert_id}><div><strong>{a.severity}</strong> · {a.asset_id}</div><p>{a.recommendation}</p><small>{a.raised_at}</small>{a.state==='OPEN'&&<button onClick={()=>onAcknowledge(a.alert_id)}>Acquitter</button>}</article>)}</section>}
