import {render,screen} from '@testing-library/react';import {describe,expect,test} from 'vitest';import {TelemetryStatus} from './TelemetryStatus';describe('TelemetryStatus',()=>{test('stale telemetry is never labelled live',()=>{render(<TelemetryStatus quality="STALE"/>);expect(screen.queryByText(/^live$/i)).not.toBeInTheDocument();expect(screen.getByText(/stale/i)).toBeInTheDocument()})});
