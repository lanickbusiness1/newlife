{
  "name": "afria-recruit-investor-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "test:e2e": "playwright test",
    "scan:bundle": "node scripts/scan-public-bundle.mjs dist",
    "package:pages": "node scripts/package-pages.mjs dist ../startup-accelerator-genome/index.html ../../public-site local",
    "check": "npm test && npm run build && npm run test:e2e && npm run scan:bundle"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.4",
    "@playwright/test": "^1.62.1",
    "vite": "^8.2.2"
  }
}
