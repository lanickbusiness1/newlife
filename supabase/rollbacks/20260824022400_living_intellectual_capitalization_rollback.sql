-- V4-DEC-016 rollback artifact.
-- WARNING: export and retain all capitalization evidence before destructive production rollback.
-- Scope is intentionally limited to the feature-owned private schema.

drop schema if exists genesis_capitalization cascade;
